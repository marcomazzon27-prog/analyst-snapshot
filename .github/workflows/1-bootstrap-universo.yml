{"name": "1-bootstrap-universo", "on": {"workflow_dispatch": null}, "permissions": {"contents": "write"}, "jobs": {"build": {"runs-on": "ubuntu-latest", "steps": [{"uses": "actions/checkout@v4"}, {"uses": "actions/setup-python@v5", "with": {"python-version": "3.11"}}, {"run": "pip install pandas lxml html5lib requests"}, {"run": "python build_universe.py"}, {"run": "git config user.name analyst-snapshot && git config user.email actions@github.com && git add universe.csv && (git diff --staged --quiet || git commit -m 'universo iniziale') && git push"}]}}}
